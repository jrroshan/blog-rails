<h1>Singe Post</h1>

<div>
  <%= image_tag @image.file, class: "img-responsive" if @image.file.present?%>
</div>

<%= render 'shared/comment_form', url: image_comments_path(@image) %>
<% @image.comments.each do |comment| %>
  <div>
    <%= comment.body %>
  </div>
<% end %>