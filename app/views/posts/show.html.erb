<h1>Singe Post</h1>

<div>
  <%= @post.title %>
</div>
<div>
  <%= @post.body %>
</div>
<%= render 'shared/comment_form', url: post_comments_path(@post) %>
<br>
<div>Comments</div>
<ul>
  <% @post.comments.each do |comment| %>
    <li>
      <%= comment.body %>
    </li>
  <% end %></ul>